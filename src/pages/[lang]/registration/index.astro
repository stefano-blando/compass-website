---
import RichContentPage from '../../../components/site/RichContentPage.astro';
import Footer from '../../../components/site/Footer.astro';
import Header from '../../../components/site/Header.astro';
import RegistrationForm from '../../../components/registration/RegistrationForm.astro';
import type { Locale } from '../../../data/types';
import Layout from '../../../layouts/Layout.astro';
import { getPageMetadata, getRichPageContent, locales } from '../../../lib/site';

export function getStaticPaths() {
  return locales.map((lang) => ({ params: { lang } }));
}

const locale = Astro.params.lang as Locale;
const pageMeta = getPageMetadata(locale, 'registration');
const content = getRichPageContent(locale, 'registration');
---

<Layout locale={locale} title={pageMeta.title} description={pageMeta.description}>
  <Header locale={locale} />
  <main class="container">
    <RichContentPage locale={locale} page="registration" content={content} />
    <RegistrationForm locale={locale} />
  </main>
  <Footer locale={locale} />
</Layout>
